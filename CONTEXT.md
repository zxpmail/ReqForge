# CONTEXT

## 当前版本
- **v1.50.0** — forge-verify LLM 协议自适应 + C2 诚实降级；此前 v1.48.6 DESIGN.md 设计链

## 上次停在哪个位置
- 全仓审计清理：loadout 计数、compile 共享模块、pre-commit sh/bat 对齐、smoke 单源

## 测试
- `pnpm test` · `pnpm forge-smoke`（15/15）· `pnpm validate-skill core/skills` · `pnpm preflight`
