# domain-mapper — Skill Eval

评估包：`triggers.json`（触发准确率）、`cases.json`（输出质量断言）、`judge-config.json`（评审维度）。

```bash
pnpm skill-eval domain-mapper
```

详见 [core/docs/skill-eval.md](../../../core/docs/skill-eval.md)。
